<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8"><title>Distribuição - Plantech</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <nav class="navbar navbar-dark p-3 mb-4">
    <div class="container">
      <span class="navbar-brand">🌱 Plantech • Logística Interna</span>
      <a href="/dashboard" class="btn btn-outline-light btn-sm">Voltar</a>
    </div>
  </nav>

  <div class="container">
    <div class="card shadow-sm">
      <div class="card-header bg-success text-white">
        <h5 class="mb-0">📋 Lista de Armazenamento (Ordem Otimizada)</h5>
      </div>
      <div class="card-body">
        <p class="text-muted">Abaixo estão os produtos recebidos que precisam ser guardados. A ordem segue do fundo do armazém para frente.</p>
        <table class="table table-hover align-middle">
          <thead>
            <tr>
              <th>Localização Alvo</th>
              <th>Produto</th>
              <th>Quantidade</th>
              <th>Ação</th>
            </tr>
          </thead>
          <tbody>
            <% lista.forEach(item => { %>
              <tr>
                <td>
                  <% if(item.numCorredor) { %>
                    <span class="badge bg-dark">Corr: <%= item.numCorredor %></span>
                    <span class="badge bg-secondary">Prat: <%= item.numPrateleira %></span>
                    <span class="badge bg-secondary">Gav: <%= item.numGaveta %></span>
                  <% } else { %>
                    <span class="badge bg-warning text-dark">Sem Local Definido</span>
                  <% } %>
                </td>
                <td class="fw-bold"><%= item.nomeProduto %></td>
                <td><%= item.quantidade %> un.</td>
                <td>
                  <a href="/distribuicao/confirmar/<%= item.idDistribuicao %>" class="btn btn-sm btn-outline-success">
                    ✅ Confirmar
                  </a>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
        <% if (lista.length === 0) { %>
          <div class="alert alert-success text-center">Tudo organizado! Nenhuma pendência.</div>
        <% } %>
      </div>
    </div>
  </div>
</body> 
</html>