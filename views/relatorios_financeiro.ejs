<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Financeiro - Plantech</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
  
  <style>
    /* --- ESTILOS DO RELATÓRIO OCULTO (PDF A4) --- */
    #relatorio-pdf {
      display: none;
      background-color: #ffffff;
      color: #000000;
      padding: 40px;
      font-family: 'Helvetica', 'Arial', sans-serif;
      font-size: 12px;
    }
    
    .pdf-header {
      border-bottom: 2px solid #2e7d32;
      padding-bottom: 15px;
      margin-bottom: 20px;
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
    }
    .pdf-title h1 { font-size: 24px; font-weight: bold; color: #2e7d32; margin: 0; }
    .pdf-title h2 { font-size: 14px; color: #555; margin: 5px 0 0 0; text-transform: uppercase; }
    .pdf-info { text-align: right; font-size: 11px; color: #666; }
    
    .pdf-box {
      border: 1px solid #ddd;
      padding: 15px;
      margin-bottom: 20px;
      background-color: #f9f9f9;
    }
    .pdf-row { display: flex; justify-content: space-between; margin-bottom: 10px; font-size: 14px; }
    .pdf-total { border-top: 1px solid #ccc; padding-top: 10px; font-weight: bold; font-size: 16px; color: #2e7d32; }
    
    .pdf-footer { margin-top: 30px; font-size: 10px; color: #777; text-align: center; border-top: 1px solid #eee; padding-top: 10px; }

    /* Estilos Tela */
    @media print { .no-print { display: none !important; } }
  </style>
</head>
<body>

  <nav class="navbar navbar-expand-lg border-bottom border-secondary border-opacity-10 no-print" data-html2canvas-ignore="true">
    <div class="container">
      <a class="navbar-brand" href="/dashboard"><span style="font-size: 1.5rem;">🌱</span> Plantech</a>
      <button class="navbar-toggler bg-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"><span class="navbar-toggler-icon"></span></button>
      <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
        <div class="nav-pills-custom">
          <a href="/dashboard" class="nav-link-custom">Dashboard</a>
          <a href="/compras" class="nav-link-custom">Compras</a>
          <a href="/vendas" class="nav-link-custom">Vendas</a>
          <a href="/produtos" class="nav-link-custom">Produtos</a>
          <a href="/clientes" class="nav-link-custom">Clientes</a>
          <a href="/fornecedores" class="nav-link-custom">Fornecedores</a>
          <a href="/relatorios" class="nav-link-custom active">Relatórios</a>
        </div>
      </div>
      <div class="d-none d-lg-block"><a href="/login/logout" class="btn btn-sm btn-outline-danger rounded-pill px-3">Sair ➔</a></div>
    </div>
  </nav>

  <div class="container pt-4">
    
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-center mb-5 no-print" data-html2canvas-ignore="true">
      <div class="mb-3 mb-md-0">
        <h2 class="mb-0 fw-bold text-white">Relatórios Gerenciais</h2>
        <p class="text-white text-opacity-50 small mb-0">DRE Simplificado e fluxo de caixa</p>
      </div>
      <div class="nav-pills-custom">
        <a href="/relatorios" class="nav-link-custom">Vendas</a>
        <a href="/relatorios/saidas" class="nav-link-custom">Saídas</a>
        <a href="/relatorios/estoque" class="nav-link-custom">Estoque</a>
        <a href="/relatorios/financeiro" class="nav-link-custom active" style="background-color: var(--accent-lime); color: #121212;">Financeiro</a>
      </div>
    </div>

    <div class="card-panel mb-5 no-print" style="background: rgba(255,255,255,0.02);" data-html2canvas-ignore="true">
      <form action="/relatorios/financeiro" method="GET" class="row g-3 align-items-end">
        <div class="col-md-5">
          <label class="form-label text-white">Data Início</label>
          <input type="date" name="dataInicio" class="form-control" value="<%= query.dataInicio %>">
        </div>
        <div class="col-md-5">
          <label class="form-label text-white">Data Fim</label>
          <input type="date" name="dataFim" class="form-control" value="<%= query.dataFim %>">
        </div>
        <div class="col-md-2">
          <button class="btn btn-primary w-100 fw-bold shadow-lg" style="height: 46px;">Atualizar</button>
        </div>
      </form>
    </div>

    <div class="row g-4 mb-5 no-print">
      <div class="col-md-4">
        <div class="stat-card bg-grad-green h-100">
          <div class="d-flex justify-content-between mb-3"><span class="badge bg-white bg-opacity-25 backdrop-blur text-white">Entradas</span><span class="fs-4">↗</span></div>
          <div><h2 class="display-5 fw-bold mb-0">R$ <%= stats.receitas.toFixed(2) %></h2><p class="mb-0 opacity-75">Total de Receitas (Vendas)</p></div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="stat-card bg-grad-red h-100">
          <div class="d-flex justify-content-between mb-3"><span class="badge bg-white bg-opacity-25 backdrop-blur text-white">Saídas</span><span class="fs-4">↘</span></div>
          <div><h2 class="display-5 fw-bold mb-0">R$ <%= stats.despesas.toFixed(2) %></h2><p class="mb-0 opacity-75">Total de Despesas (Compras)</p></div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="stat-card bg-grad-purple h-100">
          <div class="d-flex justify-content-between mb-3"><span class="badge bg-white bg-opacity-25 backdrop-blur text-white">Resultado</span><span class="fs-4">★</span></div>
          <div><h2 class="display-5 fw-bold mb-0">R$ <%= stats.lucro.toFixed(2) %></h2><p class="mb-0 opacity-75">Lucro Líquido do Período</p></div>
        </div>
      </div>
    </div>

    <div class="card-panel no-print">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h5 class="text-white mb-0">Balanço Financeiro</h5>
        <button onclick="baixarPDFFinanceiro()" class="btn btn-sm btn-outline-light rounded-pill px-3 shadow-lg" data-html2canvas-ignore="true">
          📄 Baixar Relatório Oficial
        </button>
      </div>

      <% 
         const s = (typeof stats !== 'undefined' && stats) ? stats : { receitas: 0, despesas: 0 };
         const total = s.receitas + s.despesas;
         const pRec = total > 0 ? (s.receitas / total) * 100 : 0;
         const pDesp = total > 0 ? (s.despesas / total) * 100 : 0;
      %>

      <div class="progress" style="height: 35px; background-color: #151725; border-radius: 50px; overflow: hidden;">
        <div class="progress-bar bg-grad-green" role="progressbar" style="width: <%= pRec %>%">
          <% if(pRec > 10) { %> Receita (<%= pRec.toFixed(0) %>%) <% } %>
        </div>
        <div class="progress-bar bg-grad-red" role="progressbar" style="width: <%= pDesp %>%">
          <% if(pDesp > 10) { %> Despesa (<%= pDesp.toFixed(0) %>%) <% } %>
        </div>
      </div>

      <div class="row mt-4 text-center text-white small">
        <div class="col-6 border-end border-secondary border-opacity-10">
          <span class="text-success fw-bold">● Receitas:</span> Vendas confirmadas no PDV.
        </div>
        <div class="col-6">
          <span class="text-danger fw-bold">● Despesas:</span> Compras de fornecedores lançadas.
        </div>
      </div>
    </div>

    <div class="text-center mt-5 text-white small opacity-50 no-print">
      Relatório gerado em <%= new Date().toLocaleDateString() %> às <%= new Date().toLocaleTimeString() %>
    </div>

  </div>

  <div id="relatorio-pdf">
    <div class="pdf-header">
      <div class="pdf-title">
        <h1>Plantech ERP</h1>
        <h2>Relatório Financeiro Consolidado (DRE)</h2>
      </div>
      <div class="pdf-info">
        <p><strong>Carrapicho Jardinagem</strong></p>
        <p>CNPJ: 32.665.550/0001-00</p>
        <p>Período: <%= query.dataInicio || 'Início' %> a <%= query.dataFim || 'Hoje' %></p>
        <p>Emissão: <%= new Date().toLocaleString() %></p>
      </div>
    </div>

    <div class="pdf-box">
      <h3 style="border-bottom: 1px solid #ccc; padding-bottom: 5px; margin-bottom: 15px; font-size: 14px; color: #333;">Demonstrativo de Resultados</h3>
      
      <div class="pdf-row">
        <span>(+) Receita Bruta de Vendas</span>
        <span style="color: #2e7d32;">R$ <%= stats.receitas.toFixed(2) %></span>
      </div>
      
      <div class="pdf-row">
        <span>(-) Custo de Mercadoria Vendida / Compras</span>
        <span style="color: #c62828;">(R$ <%= stats.despesas.toFixed(2) %>)</span>
      </div>
      
      <div class="pdf-row pdf-total">
        <span>(=) Lucro Líquido / Prejuízo</span>
        <span>R$ <%= stats.lucro.toFixed(2) %></span>
      </div>
    </div>

    <div style="padding: 15px; background-color: #fff3e0; border: 1px solid #ffe0b2; border-radius: 5px;">
      <strong>Análise Rápida:</strong>
      <p style="margin: 5px 0 0 0; font-size: 11px; color: #666;">
        O resultado operacional representa <%= total > 0 ? ((stats.lucro / stats.receitas) * 100).toFixed(1) : 0 %>% da receita bruta.
        <% if(stats.lucro >= 0) { %>
          A operação está superavitária neste período.
        <% } else { %>
          Atenção: As despesas superaram as receitas neste período.
        <% } %>
      </p>
    </div>

    <div class="pdf-footer">
      Sistema Plantech v2.5 - Documento para fins gerenciais.
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  
  <script>
    function baixarPDFFinanceiro() {
      const elemento = document.getElementById('relatorio-pdf');
      
      // Revela o elemento oculto apenas para o gerador
      elemento.style.display = 'block';

      const opt = {
        margin: [15, 15, 15, 15], // Margens maiores para documento formal
        filename: 'Relatorio_Financeiro_Plantech.pdf',
        image: { type: 'jpeg', quality: 0.98 },
        html2canvas: { scale: 2 }, // Alta resolução
        jsPDF: { unit: 'mm', format: 'a4', orientation: 'portrait' }
      };

      html2pdf().set(opt).from(elemento).save().then(() => {
        // Esconde de novo
        elemento.style.display = 'none';
      });
    }
  </script>

</body>
</html>