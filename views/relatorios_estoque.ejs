<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Estoque Atual - Plantech</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <style>
    /* Estilos para impressão */
    @media print {
      .no-print { display: none !important; }
      .card-panel { border: 1px solid #ddd !important; box-shadow: none !important; background: #fff !important; color: #000 !important; }
      .table { color: #000 !important; }
      body { background: #fff !important; color: #000 !important; }
      .badge { border: 1px solid #000 !important; }
    }
  </style>
</head>
<body>

  <nav class="navbar navbar-expand-lg border-bottom border-secondary border-opacity-10 no-print">
    <div class="container">
      <a class="navbar-brand" href="/dashboard">
        <span style="font-size: 1.5rem;">🌱</span> Plantech
      </a>
      <button class="navbar-toggler bg-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
        <div class="nav-pills-custom">
          <a href="/dashboard" class="nav-link-custom">Dashboard</a>
          <a href="/compras" class="nav-link-custom">Compras</a>
          <a href="/vendas" class="nav-link-custom">Vendas</a>
          <a href="/produtos" class="nav-link-custom">Produtos</a>
          <a href="/clientes" class="nav-link-custom">Clientes</a>
          <a href="/fornecedores" class="nav-link-custom">Fornecedores</a>
          <a href="/relatorios" class="nav-link-custom active">Relatórios</a> </div>
      </div>

      <div class="d-none d-lg-block">
        <a href="/login/logout" class="btn btn-sm btn-outline-danger rounded-pill px-3">Sair ➔</a>
      </div>
    </div>
  </nav>

  <div class="container pt-4">
    
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-center mb-5 no-print">
      <div class="mb-3 mb-md-0">
        <h2 class="mb-0 fw-bold text-white">Relatórios Gerenciais</h2>
        <p class="text-white text-opacity-50 small mb-0">Análise de dados e inventário</p>
      </div>
      
      <div class="nav-pills-custom">
        <a href="/relatorios" class="nav-link-custom">Vendas</a>
        <a href="/relatorios/saidas" class="nav-link-custom">Saídas</a>
        <a href="/relatorios/estoque" class="nav-link-custom active" style="background-color: var(--accent-lime); color: #121212;">Estoque</a>
        <a href="/relatorios/financeiro" class="nav-link-custom">Financeiro</a>
      </div>
    </div>

    <div class="row g-4 mb-4 no-print">
      
      <div class="col-md-4">
        <div class="stat-card bg-grad-blue h-100">
          <div class="d-flex justify-content-between mb-3">
            <span class="badge bg-white bg-opacity-25 backdrop-blur">Catálogo</span>
            <span class="fs-4">📦</span>
          </div>
          <div>
            <h2 class="fw-bold mb-0"><%= stats.totalItens %></h2>
            <p class="mb-0 opacity-75 small">Produtos Cadastrados</p>
          </div>
        </div>
      </div>

      <div class="col-md-4">
        <div class="stat-card bg-grad-red h-100">
          <div class="d-flex justify-content-between mb-3">
            <span class="badge bg-white bg-opacity-25 backdrop-blur">Crítico</span>
            <span class="fs-4">⚠️</span>
          </div>
          <div>
            <h2 class="fw-bold mb-0"><%= stats.itensZerados %></h2>
            <p class="mb-0 opacity-75 small">Itens Esgotados</p>
          </div>
        </div>
      </div>

      <div class="col-md-4">
        <div class="stat-card bg-grad-green h-100">
          <div class="d-flex justify-content-between mb-3">
            <span class="badge bg-white bg-opacity-25 backdrop-blur">Patrimônio</span>
            <span class="fs-4">💲</span>
          </div>
          <div>
            <h2 class="fw-bold mb-0">R$ <%= stats.valorPatrimonial.toFixed(2) %></h2>
            <p class="mb-0 opacity-75 small">Valor de Venda Estimado</p>
          </div>
        </div>
      </div>

    </div>

    <div class="card-panel h-100 p-0 overflow-hidden">
      <div class="p-4 border-bottom border-secondary border-opacity-10 bg-dark bg-opacity-25 d-flex justify-content-between align-items-center">
        <h5 class="text-white mb-0 fw-bold">📋 Posição de Estoque Físico</h5>
        <button onclick="window.print()" class="btn btn-sm btn-outline-light rounded-pill px-3 no-print">
          🖨️ Imprimir
        </button>
      </div>
      
      <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
          <thead class="bg-dark text-white">
            <tr>
              <th class="ps-4" style="background: #151725;">Produto</th>
              <th style="background: #151725;">Categoria</th>
              <th class="text-center" style="background: #151725;">Qtd. Atual</th>
              <th style="background: #151725;">Preço Venda</th>
              <th class="text-end" style="background: #151725;">Total</th>
              <th class="text-center pe-4" style="background: #151725;">Status</th>
            </tr>
          </thead>
          <tbody class="border-top-0">
            <% lista.forEach(p => { %>
              <% 
                 // Lógica de alerta visual
                 let statusClass = "bg-success bg-opacity-75 text-white";
                 let statusText = "Normal";
                 
                 if(p.estoqueAtual === 0) {
                    statusClass = "bg-danger bg-opacity-75 text-white";
                    statusText = "ESGOTADO";
                 } else if(p.estoqueAtual < 10) {
                    statusClass = "bg-warning bg-opacity-75 text-dark";
                    statusText = "Baixo";
                 }
              %>
              <tr>
                <td class="ps-4">
                  <strong class="text-white"><%= p.nomeProduto %></strong>
                </td>
                <td>
                  <span class="badge bg-secondary bg-opacity-25 text-white border border-secondary border-opacity-25">
                    <%= p.categoria %>
                  </span>
                </td>
                <td class="text-center">
                  <span class="fs-5 text-white fw-bold"><%= p.estoqueAtual %></span> 
                  <small class="text-white"><%= p.unidadeMedida %></small>
                </td>
                <td class="text-white">R$ <%= p.precoVenda.toFixed(2) %></td>
                <td class="text-end fw-bold text-lime">
                  R$ <%= (p.estoqueAtual * p.precoVenda).toFixed(2) %>
                </td>
                <td class="text-center pe-4">
                  <span class="badge <%= statusClass %> rounded-pill px-3">
                    <%= statusText %>
                  </span>
                </td>
              </tr>
            <% }) %>
            
            <% if(lista.length === 0) { %>
              <tr>
                <td colspan="6" class="text-center py-5 text-muted">
                  Nenhum produto no inventário.
                </td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>

    <div class="text-center mt-4 text-muted small opacity-100 no-print">
      Relatório gerado em <%= new Date().toLocaleDateString() %> às <%= new Date().toLocaleTimeString() %>
    </div>

  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>