<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Clientes - Plantech</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <style>
    /* Ajustes específicos para inputs no Dark Mode */
    .form-control, .form-select {
      background-color: #12141e !important;
      border: 1px solid rgba(255, 255, 255, 0.1) !important;
      color: #ffffff !important;
    }
    .form-control:focus, .form-select:focus {
      border-color: var(--accent-lime) !important;
      box-shadow: 0 0 0 2px rgba(204, 243, 129, 0.2) !important;
    }
    .form-label {
      color: #e0e0e0;
      font-weight: 500;
      font-size: 0.9rem;
    }
    ::placeholder {
      color: rgba(255, 255, 255, 0.3) !important;
    }
    
    /* Scroll personalizado na tabela */
    .table-container {
      max-height: 70vh;
      overflow-y: auto;
    }
    .table-container::-webkit-scrollbar { width: 6px; }
    .table-container::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.1); border-radius: 10px; }

    /* Classes para os Avatares (Corrige o erro de sintaxe anterior) */
    .avatar-base {
      width: 45px;
      height: 45px;
      color: #121212; /* Texto preto para contraste */
    }
    .avatar-pj {
      background-color: var(--accent-lime); /* Verde Neon */
    }
    .avatar-pf {
      background-color: #e0e0e0; /* Cinza Claro */
    }
  </style>
</head>
<body>

  <nav class="navbar navbar-expand-lg border-bottom border-secondary border-opacity-10">
    <div class="container">
      <a class="navbar-brand" href="/dashboard">
        <span style="font-size: 1.5rem;">🌱</span> Plantech
      </a>

      <button class="navbar-toggler bg-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
        <div class="nav-pills-custom">
          <a href="/dashboard" class="nav-link-custom">Dashboard</a>
          <a href="/compras" class="nav-link-custom">Compras</a>
          <a href="/vendas" class="nav-link-custom">Vendas</a>
          <a href="/produtos" class="nav-link-custom">Produtos</a>
          <a href="/clientes" class="nav-link-custom active">Clientes</a> <a href="/fornecedores" class="nav-link-custom">Fornecedores</a>
          <a href="/relatorios" class="nav-link-custom">Relatórios</a>
        </div>
      </div>

      <div class="d-none d-lg-block">
        <a href="/login/logout" class="btn btn-sm btn-outline-danger rounded-pill px-3">Sair ➔</a>
      </div>
    </div>
  </nav>

  <div class="container pt-4">
    
    <div class="d-flex justify-content-between align-items-center mb-4">
      <div>
        <h2 class="mb-0 fw-bold text-white">Carteira de Clientes</h2>
        <p class="text-white text-opacity-50 small mb-0">Gerencie consumidores Pessoa Física e Jurídica</p>
      </div>
      <span class="badge bg-white bg-opacity-10 text-white px-3 py-2 rounded-pill border border-secondary border-opacity-25">
        Total: <%= lista.length %>
      </span>
    </div>

    <% if (erro) { %>
      <div class="alert alert-danger rounded-4 mb-4 text-center border-0 bg-danger bg-opacity-25 text-white"><%= erro %></div>
    <% } %>

    <div class="row g-4">
      
      <div class="col-lg-4">
        <div class="card-panel h-100 bg-grad-dark" style="border: 1px solid rgba(255,255,255,0.05);">
          <div class="d-flex align-items-center gap-2 mb-4 text-lime">
            <span class="fs-5">✨</span> 
            <h5 class="mb-0 fw-bold">Novo Cliente</h5>
          </div>
          
          <form action="/clientes/add" method="POST" class="row g-3">
            
            <div class="col-12">
              <label class="form-label">Tipo de Pessoa</label>
              <select name="tipo" id="tipoCliente" class="form-select text-center fw-bold text-white" onchange="alternarTipo()">
                <option value="PF">Pessoa Física (CPF)</option>
                <option value="PJ">Pessoa Jurídica (CNPJ)</option>
              </select>
            </div>

            <div class="col-12">
              <label class="form-label" id="labelNome">Nome Completo</label>
              <input type="text" name="nome" class="form-control" required placeholder="Ex: João da Silva">
            </div>

            <div class="col-12" id="divFantasia" style="display:none;">
              <label class="form-label">Nome Fantasia</label>
              <input type="text" name="nomeFantasia" class="form-control" placeholder="Ex: Mercado Silva">
            </div>

            <div class="col-12">
              <label class="form-label" id="labelDocumento">CPF</label>
              <input type="text" name="documento" class="form-control" required placeholder="Apenas números">
            </div>

            <div class="col-6">
              <label class="form-label">Telefone</label>
              <input type="text" name="telefone" class="form-control" placeholder="(00) 00000-0000">
            </div>

            <div class="col-6">
              <label class="form-label">Email</label>
              <input type="email" name="email" class="form-control" placeholder="cliente@email.com">
            </div>

            <div class="col-12">
              <label class="form-label">Endereço Completo</label>
              <textarea name="endereco" class="form-control" rows="2" required placeholder="Rua, Número, Bairro..."></textarea>
            </div>

            <div class="col-12 mt-4">
              <button class="btn btn-primary w-100 py-3 shadow-lg rounded-pill fw-bold">
                <span class="fs-6">💾</span> Salvar Cadastro
              </button>
            </div>

          </form>
        </div>
      </div>

      <div class="col-lg-8">
        <div class="card-panel h-100 p-0 overflow-hidden">
          <div class="p-4 border-bottom border-secondary border-opacity-10 bg-dark bg-opacity-25">
            <h5 class="text-white mb-0 fw-bold">📇 Base de Cadastros</h5>
          </div>
          
          <div class="table-container p-0">
            <table class="table table-hover align-middle mb-0">
              <thead class="bg-dark text-white sticky-top" style="z-index: 2;">
                <tr>
                  <th class="ps-4" style="background: #151725;">Identificação</th>
                  <th style="background: #151725;">Documento</th>
                  <th style="background: #151725;">Contato</th>
                  <th class="text-end pe-4" style="background: #151725;">Ação</th>
                </tr>
              </thead>
              <tbody class="border-top-0">
                <% lista.forEach(c => { %>
                  <tr>
                    <td class="ps-4 py-3">
                      <div class="d-flex align-items-center gap-3">
                        
                        <div class="rounded-circle d-flex align-items-center justify-content-center fw-bold shadow-sm flex-shrink-0 avatar-base <%= c.tipo == 'PJ' ? 'avatar-pj' : 'avatar-pf' %>">
                          <%= c.tipo %>
                        </div>

                        <div style="line-height: 1.3;">
                          <strong class="text-white d-block"><%= c.nome %></strong>
                          <% if(c.tipo == 'PJ' && c.nomeFantasia){ %>
                            <small class="text-white text-opacity-50"><%= c.nomeFantasia %></small>
                          <% } else { %>
                            <small class="text-white text-opacity-50 text-truncate d-block" style="max-width: 200px;">
                              📍 <%= c.endereco %>
                            </small>
                          <% } %>
                        </div>
                      </div>
                    </td>
                    
                    <td>
                      <span class="badge bg-dark border border-secondary border-opacity-25 text-white fw-normal">
                        <%= c.documento %>
                      </span>
                    </td>
                    
                    <td class="small text-white text-opacity-75">
                      <% if(c.telefone) { %> <div class="mb-1">📞 <%= c.telefone %></div> <% } %>
                      <% if(c.email) { %> <div>✉️ <%= c.email %></div> <% } %>
                    </td>
                    
                    <td class="text-end pe-4">
                      <a href="/clientes/delete/<%= c.idCliente %>" 
                         class="btn btn-sm btn-outline-danger rounded-pill px-3"
                         title="Excluir Cliente"
                         onclick="return confirm('Tem certeza?');">
                        🗑️
                      </a>
                    </td>
                  </tr>
                <% }) %>
                
                <% if(lista.length === 0) { %>
                  <tr>
                    <td colspan="4" class="text-center py-5">
                      <div class="opacity-25 mb-2" style="font-size: 3rem;">📂</div>
                      <p class="text-white text-opacity-50">Nenhum cliente cadastrado ainda.</p>
                    </td>
                  </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>

    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    function alternarTipo() {
      const tipo = document.getElementById('tipoCliente').value;
      const labelNome = document.getElementById('labelNome');
      const labelDoc = document.getElementById('labelDocumento');
      const divFantasia = document.getElementById('divFantasia');

      if (tipo === 'PF') {
        labelNome.innerText = "Nome Completo";
        labelDoc.innerText = "CPF";
        divFantasia.style.display = "none";
      } else {
        labelNome.innerText = "Razão Social";
        labelDoc.innerText = "CNPJ";
        divFantasia.style.display = "block";
      }
    }
  </script>

</body>
</html>